# Backend do ARKHER AI — dependências mínimas e first-party.
# Nenhuma biblioteca de cliente de provedor de IA externo existe aqui.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
httpx==0.28.1          # usado apenas em testes de integração
pytest==9.1.1
PyYAML==6.0.3
# torch é instalado separadamente (ver docs/TRAINING.md / Dockerfile):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
